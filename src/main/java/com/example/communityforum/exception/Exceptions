package com.example.communityforum.exception;

import org.springframework.http.HttpStatus;

public final class Exceptions {
    private Exceptions() {}

    public static HttpStatusException exception(String message, HttpStatus status) {
        return HttpStatusException.of(message, status);
    }
}